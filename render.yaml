
# databases:
#   - name: mysitedb
#     plan: free
#     database: mysql
#     user: mysite

services:
  - type: web
    plan: free
    name: wearvault
    runtime: python
    buildCommand: "./build.sh"
    startCommand: "python -m gunicorn wearvault.wsgi:application"
    # envVars:
    #   - key: DATABASE_URL
    #     fromDatabase:
    #       name: mysitedb
    #       property: connectionString
    #   - key: SECRET_KEY
    #     generateValue: true
    #   - key: WEB_CONCURRENCY
    #     value: 4